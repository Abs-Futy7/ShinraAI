# Shinrai Frontend

Next.js 14 (App Router) frontend for the PRD → Blog agent pipeline.

## Setup

```bash
npm install
npm run dev -- --port 3000
```

Requires the backend running on `http://localhost:8000` (configurable via `BACKEND_URL` in `.env.local`).
