# ===================================================================
# API KEYS (Provide at least one)
# ===================================================================

# Groq API Key (Fast, but NO web search support)
GROQ_API_KEY=your_groq_api_key_here

# Google Gemini API Key (Supports web search with function calling)
# Both variables should have the same value for compatibility
GEMINI_API_KEY=your_gemini_api_key_here
GOOGLE_API_KEY=your_gemini_api_key_here

# ===================================================================
# MODEL SELECTION (Optional - Can be configured per-run in frontend)
# ===================================================================
# Frontend allows per-run model selection. Set MODEL here for default/fallback.
# Groq models: groq/llama-3.1-8b-instant (fast), groq/llama-3.3-70b-versatile
# Gemini models: gemini/gemini-2.5-flash (recommended), gemini/gemini-1.5-flash, gemini/gemini-1.5-pro

MODEL=gemini/gemini-2.5-flash  # Default model if not specified in frontend

# ===================================================================
# WEB SEARCH (Optional - Only works with Gemini models)
# ===================================================================
SERPER_API_KEY=your_serper_api_key_here

# ===================================================================
# OTHER SETTINGS
# ===================================================================
CREWAI_TRACING_ENABLED=false

# ===================================================================
# DATABASE (Supabase Postgres)
# ===================================================================
# Example:
# DATABASE_URL=postgresql://postgres:<PASSWORD>@db.<PROJECT-REF>.supabase.co:5432/postgres?sslmode=require
DATABASE_URL=
# Supabase JWT secret (Project Settings -> API -> JWT Secret)
SUPABASE_JWT_SECRET=your_supabase_jwt_secret_here

# ===================================================================
# RUBRIC QUALITY GATE (Optional)
# ===================================================================
# Score scale is 1-5. These thresholds control pass/fail.
RUBRIC_MIN_CLARITY=3
RUBRIC_MIN_CORRECTNESS=4
RUBRIC_MIN_COMPLETENESS=2
RUBRIC_MIN_OVERALL=3
# Number of automatic rollback retries to writer/fact-check/style
RUBRIC_MAX_RETRIES=1
# Optional explicit model for grader step. Defaults to model selected for run.
# RUBRIC_MODEL=gemini/gemini-2.5-flash
